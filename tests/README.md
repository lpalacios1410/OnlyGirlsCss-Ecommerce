# Tests (Playwright)

Este directorio contiene las pruebas end-to-end y automatizadas usando Playwright.

## Scripts principales

- `npm install`: Instala las dependencias de testing.
- `npx playwright test`: Ejecuta los tests.

## Notas

- La configuración principal está en `playwright.config.js`.
- Los tests están en la subcarpeta `tests/`.
